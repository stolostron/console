/* Copyright Contributors to the Open Cluster Management project */

export const fetchHubClusterName = () => {
  return Promise.resolve('local-cluster') // TODO - connect to backend route to fetch hub cluster name
}
