query clusterImageSets($labelSelector: String) {
    clusterImageSets(labelSelector: $labelSelector) {
        metadata {
            uid
            name
            namespace
            labels
        }
    }
}
