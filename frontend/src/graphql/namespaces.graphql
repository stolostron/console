query namespaces($labelSelector: String) {
    namespaces(labelSelector: $labelSelector) {
        metadata {
            uid
            name
            namespace
            labels
        }
    }
}
