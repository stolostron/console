query bareMetalAssets($labelSelector: String) {
    bareMetalAssets(labelSelector: $labelSelector) {
        metadata {
            uid
            name
            namespace
            labels
        }
    }
}
