overwrite: true
schema: '../backend/schema.gql'
documents: './src/graphql/*.graphql'
generates:
    src/sdk.ts:
        plugins:
            - 'typescript'
            - 'typescript-operations'
            - typescript-react-apollo
        config:
            skipTypename: true
