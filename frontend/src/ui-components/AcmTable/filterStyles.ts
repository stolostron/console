/* Copyright Contributors to the Open Cluster Management project */
import { css } from '@emotion/css'

export const filterLabelMargin = css({
  marginRight: '.5rem',
})

export const filterOption = css({
  display: 'flex',
  alignItems: 'center',
})

export const filterOptionBadge = css({
  marginLeft: '.5rem',
})
