# ACM-32324 UI evidence (after fix)

Playwright screenshots captured against repro clusters on the dedicated test hub after the AnsibleWorkflow fix.

- **Phase:** `after` (see `after/manifest.json` for per-scenario metadata and View logs navigation URLs)
- **Captured:** 2026-08-20
- **Hub label:** `acm-32324-repro=true`

Each scenario folder prefix (`01`–`06`) matches `scripts/acm-32324-ui-repro/scenarios.json` in the PR branch (local repro tooling is optional for reviewers).
